@{
    ViewData["Title"] = "Đăng Nhập";
    Layout = null;
}

<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - CookMate</title>
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style>
        body {
            margin: 0;
            padding: 0;
            background-color: #E0CEC8;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            font-family: 'Inter', sans-serif;
        }

        .login-container {
            background: white;
            border-radius: 20px;
            padding: 40px;
            box-shadow: 0px 4px 4px rgba(0, 0, 0, 0.25);
            width: 100%;
            max-width: 400px;
            text-align: center;
            border: 2px solid #090808;
        }

        .logo-section {
            margin-bottom: 30px;
        }

        .logo-img {
            width: 80px;
            height: 80px;
            margin-bottom: 15px;
        }

        .app-name {
            font-size: 32px;
            font-weight: 700;
            color: #2c3e50;
            margin: 0;
        }

        .welcome-text {
            font-size: 18px;
            color: #7f8c8d;
            margin: 20px 0 30px 0;
        }

        .login-buttons {
            display: flex;
            flex-direction: column;
            gap: 15px;
        }

        .login-btn {
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 15px 20px;
            border: none;
            border-radius: 12px;
            font-size: 16px;
            font-weight: 500;
            cursor: pointer;
            transition: all 0.3s ease;
            text-decoration: none;
            gap: 12px;
        }

        .google-btn {
            background: white;
            color: #333;
            border: 2px solid #e0e0e0;
        }

        .google-btn:hover {
            background: #f5f5f5;
            border-color: #d0d0d0;
        }

        .email-btn {
            background: #333;
            color: white;
        }

        .email-btn:hover {
            background: #555;
        }

        .google-icon {
            width: 20px;
            height: 20px;
        }

        .email-icon {
            width: 20px;
            height: 20px;
        }

        .divider {
            display: flex;
            align-items: center;
            margin: 20px 0;
        }

        .divider-line {
            flex: 1;
            height: 1px;
            background: #e0e0e0;
        }

        .divider-text {
            margin: 0 15px;
            color: #999;
            font-size: 14px;
        }

        .error-message {
            background: #ffebee;
            color: #c62828;
            padding: 12px;
            border-radius: 8px;
            margin-bottom: 20px;
            font-size: 14px;
        }

        .top-left-text {
            position: absolute;
            top: 20px;
            left: 20px;
            color: #817B7B;
            font-size: 18px;
            font-weight: 600;
        }

        .top-right-btn {
            position: absolute;
            top: 20px;
            right: 20px;
            background: white;
            color: #000;
            border: 2px solid #090808;
            padding: 10px 20px;
            border-radius: 20px;
            font-size: 14px;
            cursor: pointer;
            transition: all 0.3s ease;
            box-shadow: 0px 4px 4px rgba(0, 0, 0, 0.25);
        }

        .top-right-btn:hover {
            background: #f0f0f0;
            transform: translateY(-2px);
        }
    </style>
</head>
<body>
    <div class="top-left-text">Đăng Nhập</div>

    <div class="login-container">
        <div class="logo-section">
            <img src="~/images/logos/chef-hat.png" alt="CookMate Logo" class="logo-img">
            <h1 class="app-name">CookMate</h1>
        </div>

        <p class="welcome-text">Welcome to CookMate</p>

        @if (TempData["Error"] != null)
        {
            <div class="error-message">
                @TempData["Error"]
            </div>
        }

        <div class="login-buttons">
            <button class="login-btn google-btn" onclick="loginWithGoogle()">
                <svg class="google-icon" viewBox="0 0 24 24">
                    <path fill="#4285F4" d="M22.56 12.25c0-.78-.07-1.53-.2-2.25H12v4.26h5.92c-.26 1.37-1.04 2.53-2.21 3.31v2.77h3.57c2.08-1.92 3.28-4.74 3.28-8.09z"/>
                    <path fill="#34A853" d="M12 23c2.97 0 5.46-.98 7.28-2.66l-3.57-2.77c-.98.66-2.23 1.06-3.71 1.06-2.86 0-5.29-1.93-6.16-4.53H2.18v2.84C3.99 20.53 7.7 23 12 23z"/>
                    <path fill="#FBBC05" d="M5.84 14.09c-.22-.66-.35-1.36-.35-2.09s.13-1.43.35-2.09V7.07H2.18C1.43 8.55 1 10.22 1 12s.43 3.45 1.18 4.93l2.85-2.22.81-.62z"/>
                    <path fill="#EA4335" d="M12 5.38c1.62 0 3.06.56 4.21 1.64l3.15-3.15C17.45 2.09 14.97 1 12 1 7.7 1 3.99 3.47 2.18 7.07l3.66 2.84c.87-2.6 3.3-4.53 6.16-4.53z"/>
                </svg>
                Login with Google
            </button>

            <div class="divider">
                <div class="divider-line"></div>
                <span class="divider-text">OR</span>
                <div class="divider-line"></div>
            </div>

            <button class="login-btn email-btn" onclick="showEmailLogin()">
                <svg class="email-icon" fill="currentColor" viewBox="0 0 24 24">
                    <path d="M20 4H4c-1.1 0-1.99.9-1.99 2L2 18c0 1.1.9 2 2 2h16c1.1 0 2-.9 2-2V6c0-1.1-.9-2-2-2zm0 4l-8 5-8-5V6l8 5 8-5v2z"/>
                </svg>
                Login with Email
            </button>
        </div>
    </div>

    <!-- Email Login Modal -->
    <div id="emailModal" style="display: none; position: fixed; top: 0; left: 0; width: 100%; height: 100%; background: rgba(0,0,0,0.5); z-index: 1000;">
        <div style="position: absolute; top: 50%; left: 50%; transform: translate(-50%, -50%); background: white; padding: 30px; border-radius: 15px; width: 90%; max-width: 400px;">
            <h3 style="margin-top: 0; text-align: center;">Đăng Nhập với Email</h3>
            <p style="text-align: center; color: #666; margin-bottom: 20px;">Nhập email để nhận mã OTP</p>
            <form method="post" action="@Url.Action("SendOTP", "Auth")">
                <div style="margin-bottom: 20px;">
                    <input type="email" name="email" placeholder="Nhập email của bạn" required style="width: 100%; padding: 12px; border: 2px solid #e0e0e0; border-radius: 8px; font-size: 16px; box-sizing: border-box;">
                </div>
                <div style="display: flex; gap: 10px;">
                    <button type="submit" style="flex: 1; padding: 12px; background: #007bff; color: white; border: none; border-radius: 8px; font-size: 16px; cursor: pointer;">Gửi Mã OTP</button>
                    <button type="button" onclick="closeEmailModal()" style="flex: 1; padding: 12px; background: #6c757d; color: white; border: none; border-radius: 8px; font-size: 16px; cursor: pointer;">Hủy</button>
                </div>
            </form>
            <div style="text-align: center; margin-top: 15px;">
                <p style="color: #666; font-size: 14px;">Mã OTP sẽ được gửi đến email của bạn</p>
            </div>
        </div>
    </div>

    <script>
        function loginWithGoogle() {
            alert('Tính năng đăng nhập Google sẽ được triển khai sau!');
        }

        function showEmailLogin() {
            document.getElementById('emailModal').style.display = 'block';
            // Focus on email input
            setTimeout(() => {
                document.querySelector('#emailModal input[type="email"]').focus();
            }, 100);
        }

        function closeEmailModal() {
            document.getElementById('emailModal').style.display = 'none';
            // Clear form
            document.querySelector('#emailModal form').reset();
        }

        // Đóng modal khi click outside
        document.getElementById('emailModal').addEventListener('click', function(e) {
            if (e.target === this) {
                closeEmailModal();
            }
        });

        // Handle form submission
        document.querySelector('#emailModal form').addEventListener('submit', function(e) {
            const email = this.querySelector('input[type="email"]').value;
            if (!email) {
                e.preventDefault();
                alert('Vui lòng nhập email');
                return;
            }
            
            // Show loading
            const submitBtn = this.querySelector('button[type="submit"]');
            const originalText = submitBtn.textContent;
            submitBtn.textContent = 'Đang gửi...';
            submitBtn.disabled = true;
            
            // Re-enable button after 3 seconds (in case of error)
            setTimeout(() => {
                submitBtn.textContent = originalText;
                submitBtn.disabled = false;
            }, 3000);
        });
    </script>
</body>
</html>
